<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="action_repair_purchase_uop_before_invoice" model="ir.actions.server">
            <field name="name">Revisar datos antes de crear factura</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_type">action</field>
            <field name="state">code</field>
            <field name="code">action = records.action_repair_uop_before_invoice()</field>
        </record>
    </data>
</odoo>
